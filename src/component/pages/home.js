import React from "react";
import SVSViewer from "../SVSViewer";

const Home = () => {
    return (
        <div>
            <h1 style={{ textAlign: "center" }}>SVS Image Viewer</h1>
            <SVSViewer />
        </div>
    );
};

export default Home;
